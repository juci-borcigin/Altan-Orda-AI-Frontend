# 講義メーカー — 方針メモ（2026-07-20）

正本のフォーマット前提: **`AO_Course_Format_v2.md`**（Web記事型・2026-07-20 確定）。  
本ドキュメントはモデル結論・Sample／構成フェーズ拡張の経緯メモ。

**サブモジュール・クローズ（2026-07-31）:** 入口は **`README.md`**。AO 本体整備へ移行。再開時も README から。

---

## 1. モデル／画像テスト結論（確定・Format v2 でも維持）

| 役割 | 採用 | 備考 |
|------|------|------|
| 構成・指示文 | GPT-5.6 **Terra** | outline／固い指示 |
| 本文執筆 | GPT-5.6 **Luna** | 不安時・フォールバックは Terra |
| 回メイン画像 | GPT Image 2 **Low** | 回あたり1枚・横長。Mid は不要 |
| セクション画像 | **ネット取得（非生成）** | Format v2。旧「セクションごと生成」は破棄 |
| Sonnet 系（本文） | **不採用** | コスパが劣る |

- これまでのモデル調査（text-lab／gpt-5-6-lab／session1-visual-lab）は **生きる**。
- Sonnet 再実行・Terra sec2 Low 差し替えは不要。
- `.env.example` の Terra outline / Luna session / Image2 Low は本結論と一致。コードの全面切替は Format v2 実装時に行う。

---

## 1b. Format v2 への移行（要約）

詳細は `AO_Course_Format_v2.md`。

- 紙芝居破棄 → **Web／ブログ記事**
- 1回＝**5000字固定**、指定は **回数5〜10のみ**
- 属性: キッズ／学生／社会人（当面社会人）。数学は小中学固定
- 画像: 回メイン＝Image2 Low 生成1枚、セクション＝検索の小画像、Intro/Outroは画像なし
- 中身セクション目安 **5±1**（8は多すぎ）

旧 `分 × 200字` の本文目標は破棄（読了目安表示には転用可）。

---

## 2. スマホで Lab を見る（Preview 本命）

旧称 Sample（`/sample`）。2026-07-31 より公式パスは **`/lab`（実験室）**。詳細は `docs/lab/README.md`。

### 2.1 「`/lab` 以下だけ本番 Push」はできるか

**同一 Next.js 本番プロジェクトに対し、パス単位で一部だけデプロイする機能は Vercel にない。**

| やり方 | 可否 | 実態 |
|--------|------|------|
| 本番に `/lab` だけ載せる | △ | アプリ全体がビルド・デプロイされる。ルートを隠しても成果物は全ルート含む |
| Ignored Build Step で lab 変更時だけ本番ビルド | ○だが別物 | 「本番を汚す／汚さない」の制御であり、パス限定デプロイではない |
| **Preview デプロイ（専用ブランチ）** | ◎ | Mac 不要の URL。本番ドメインを触らない |
| `public/lab` 静的ホスト（Pages / Netlify Drop） | ◎ | 閲覧専用・最安。生成 API なし |
| Lab 専用の別 Vercel プロジェクト | ○ | Root を絞っても Next 構成次第では同じ `web` 全体ビルドになりがち。静的 export 向き |

**結論:** 「一部本番 Push」より **Preview（本命）＋必要なら静的ミラー** が正しい。

### 2.2 Preview 実装上の注意（問題になりうる点）

実装自体は可能。先に潰すべきは次。

1. **課金 API** — `/api/lab/**` の生成系は Preview でも鍵があれば叩ける。Preview では無効化、または秘密トークン必須にする。
2. **成果物サイズ** — `public/lab` の PNG（数十 MB〜）がデプロイを重くする。比較用は必要なラボだけ残すか、外部ストレージ参照を検討。
3. **認証** — 本番と同じ Google OAuth / Basic が Preview に掛かるとスマホ閲覧が詰まる。Lab 閲覧は許可メール or Lab のみ公開の方針を決める。
4. **環境変数** — 閲覧だけなら LLM 鍵は不要。生成を Preview で回すなら鍵と課金上限の明示が必要。
5. **本番ナビ汚染** — Preview ブランチを `main` にマージすると本番にも `/lab` が出る。恒久公開するか、ブランチ運用で分けるか決める。

**推奨オペレーション（案）**

1. lab 用ブランチを切る（または PR の Preview URL を使う）。
2. 生成 API を Preview でガード。
3. スマホは `https://…vercel.app/lab` で閲覧。
4. Mac オフ・長期保管は `session1-visual-lab-offline.zip` / 静的ホストを併用。

### 2.3 Preview 運用（実装済み）

コード:

- `web/src/lib/course-maker/lab-api-guard.ts` — POST `/api/lab/**` ガード
- `web/src/auth.ts` — `AO_LAB_PUBLIC=1`（互換 `AO_SAMPLE_PUBLIC`）で `/lab` と GET `/api/lab/*` を公開
- `next.config.ts` — `/sample` → `/lab` リダイレクト

Vercel Preview 推奨 env:

```
AO_LAB_PUBLIC=1
# 生成を Preview で回すときのみ:
# AO_LAB_API_SECRET=<長い乱数>
# 生成を完全に閉じる:
AO_LAB_API_DISABLED=1
```

（既存の `AO_SAMPLE_*` もデュアルリードで有効。）

手順:

1. ブランチで PR を開き、Vercel Preview URL を取得する
2. Preview の Environment Variables に上記を設定する（Production とは分離）
3. スマホで `https://<preview>.vercel.app/lab` を開く
4. POST 生成は秘密ヘッダ `x-ao-lab-secret`（または `x-ao-sample-secret`）なしでは拒否（Vercel 上は秘密未設定でも拒否）

---

## 3. ベース知識・信頼性 — 講義構成フェーズの再定義

### 3.1 提案フロー（殿下案）

```
1. モデルがコンテンツ・ドラフトを作成
2. 監査
   (1) 追加すべき最新情報を調査
   (2) コンテンツ・ドラフトのファクトチェック
3. コンテンツ・ドラフトを改訂・確定
4. 確定ドラフトをベースに、回数・時間から講義を構成
5. 回・セクションの本文出力
```

旧 Tier 1（講義構成）を **「知識ドラフト → 監査 → 確定 → 構成」** に拡張するイメージ。

補足（2026-07-20）: 知識ドラフトの見出しは「教える中身の目次」であり、受講画面の講義セクションではない。講義セクションはステップ4で、ユーザー指定の回数・時間から設計する（上限10・目安400字）。

### 3.2 方向性の妥当性

**妥当。推奨方向。**（2026-07-20 合意: ドラフト＝講義全体、監査＝要点＋上限付きで PoC 設計へ）

詳細スキーマ・上限・完了条件: `AO_Course_Foundation_PoC.md`

### 3.3 ツール／モデル割当（提案）

| ステップ | 提案 | 代替・メモ |
|----------|------|------------|
| **1 ドラフト** | **Luna** | 広く・安く草案。想像どおりでよい |
| **2 監査（調査＋FC）** | **Tavily（検索）+ Luna または Terra（判定）** | 下記 |
| **3 改訂・確定** | **Terra**（推奨） | 確定文は固く。軽量なら Luna でも可 |
| **4 講義構成** | **Terra** | 構造化・制約遵守向き。現状 outline と一致 |
| **5 回本文** | **Luna**（主）/ Terra フォールバック | テスト結論どおり |
| 画像 | Image 2 **Low** | テスト結論どおり |

#### ステップ2: Tavily vs Perplexity

| | Tavily | Perplexity Search API |
|--|--------|------------------------|
| 役割 | 生検索（ヒット URL・スニペット） | 検索＋要約寄りの結果 |
| AO との関係 | チャット経路で実績あり | **Tavily の将来比較・代替候補**（本文モデルとの比較対象ではない） |
| 講義監査での向き | **主候補**: クエリ制御・引用 URL が取りやすい | 監査の「調査」脚を短くしたいときの候補 |

**提案:** 当面 **Tavily を監査の調査脚**。判定・矛盾指摘・追記指示は **Terra（厳しめ）または Luna（安め）**。  
最初の PoC は「Tavily + Terra 判定」で確度優先、コストが見えたら判定を Luna に落とす。

Perplexity は「同じ監査ジョブを Tavily 経路と差し替えて品質・単価比較」用に残す（ユーザー規則どおり本文モデル比較には使わない）。

### 3.4 監査のパフォーマンス影響

殿下の見立て（**回・セクション本文には大きな影響がない**）は **概ね正しい**。

- 監査は **講義あたり数回（ドラフト確定まで）** で、ステップ5の N 回 × セクション生成とは直交する。
- 支配要因はステップ5＋画像。監査は相対的に小さい。

ただし監査を甘く見ない条件:

- ドラフトが超長文＋「全主張を個別検索」にすると、**検索回数 × LLM** で構成フェーズだけが高騰する。
- **上限を決める**: 例) 調査クエリ最大 N、監査 LLM 1〜2 呼び出し、対象は「数値・固有名・時事・論争点」のみ。

---

## 4. 出典（インライン参照）

- 「この部分の詳細はここを参照」を **セクション単位で確定**するのは、上記フローでも難しい。
  - 監査で URL は集まるが、ステップ5の本文がどの文に紐づくかの **対応付けが別問題**。
- 監査成果物として **講義レベルの参考リンク一覧** を残すのは比較的容易（Nice）。
- **結論:** インライン出典・「詳細はこちら」は **Nice to Have としてドロップ可**。必要になったら「講義末尾の参考リスト」から再開する。

---

## 5. 状態（2026-07-31 クローズ）

| 項目 | 状態 |
|------|------|
| モデル／画像テスト結論 | 確定（本文書 §1） |
| Lab（旧 Sample）Preview + API ガード | 実装済み（§2.3）。正本 `docs/lab/README.md` |
| Format v2 実装（作成・学習・公開 `/l`） | **実装済み・クローズ** → `README.md` |
| 構成フェーズ 1→5 | PoC／設計。本番必須導線ではない |
| 出典インライン | ドロップ |
| AO 本体への埋め込み | **未着手（再開バックログ）** |
| 一括生成でヒーロー同梱 | **未修正（再開バックログ）** |

詳細・コード地図・共有 URL: **`README.md`**

---

## 6. 残タスク（クローズ後バックログ）

講義メーカーを触る仕事は一旦止め、AO 本体へ移る。再開時のみ:

1. 「全回一括」既定で回メイン画像も出す（または明示ボタン）
2. 「本文を作成」＝ CourseMaster＋本文の本番一気通貫
3. AO 製品内への講義表示（パターン1）
4. Wikimedia 権利・セーフサーチの厳密化（必要なら）
5. Foundation（知識ドラフト〜監査）を本番前段に入れるか再判断
6. 出典インラインは引き続き見送り可

旧スナップショット（2026-07-20）の「Format v2 切替」は完了済み。

